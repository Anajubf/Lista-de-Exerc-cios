let inscritos = [
    "Pedro", "Larissa", "João", "Amanda", "Lucas", "Mariana"
  ];

  let inscricaoRemovida  = inscritos.pop();

console.log(`Depois do pop: ${inscritos}`);
console.log(`A inscrição removida foi: ${inscricaoRemovida}`);